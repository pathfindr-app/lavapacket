# Supabase Configuration
# Copy this file to .env.local and fill in your values

# Supabase Project URL (from Settings > API)
SUPABASE_URL=https://your-project.supabase.co

# Supabase Anonymous Key (from Settings > API)
SUPABASE_ANON_KEY=your-anon-key-here

# Note: For Vercel deployment, set these as Environment Variables:
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY

# To configure the app with Supabase:
# 1. Create a Supabase project at https://supabase.com
# 2. Run the SQL from supabase-schema.sql in the SQL Editor
# 3. Create a 'photos' storage bucket with public access
# 4. Copy your project URL and anon key here
# 5. Update the meta tags in HTML files OR inject via build process
